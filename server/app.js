

const createHTTPServer = require('./httpServer');

// Create an HTTP server that supports WebSocket upgrade requests and handshakes (RFC 6455).
const HTTP_SERVER = createHTTPServer();




















